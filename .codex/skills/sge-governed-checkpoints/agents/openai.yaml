interface:
  display_name: "SGE Governed Checkpoints"
  short_description: "Apply SGE governance gates to complex repo work"
  default_prompt: "Use $sge-governed-checkpoints to run a governed intake, validation, and closeout for this task."
policy:
  allow_implicit_invocation: true
